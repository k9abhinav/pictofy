<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.1.0/fonts/remixicon.css" rel="stylesheet" />
</head>

<body>
    <% if(nav === true){ %>
        <div class="w-full px-5 py-3 flex flex-col md:flex-row justify-between bg-zinc-700 text-white items-center">
            <h3 class="text-xl font-semibold">Pictofy.</h3>
            <div class="flex flex-col md:flex-row gap-5 md:gap-10 items-center mt-3 md:mt-0">
                <a href="/profile" class="hover:text-gray-300">Profile</a>
                <a href="/feed" class="hover:text-gray-300">Feed</a>
                <a href="/logout" class="px-4 py-2 bg-red-700 text-white rounded-lg hover:bg-red-600 transition">Logout</a>
            </div>
        </div>
        
    <% } %>